import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: " roboto, sans-serif",
  },
  palette: {
    mode: "light",
    text: {
      primary: "#000000",
      secondary: "#000000",
    },
    primary: {
      main: "#92ce04",
    },
    secondary: {
      main: "#92ce04",
    },
    background: {
      default: "#FFFFFF",
      paper: "#FFFFFF",
    },
  },
});
